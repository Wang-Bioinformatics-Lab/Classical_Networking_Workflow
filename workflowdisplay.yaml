name: classical_networking_workflow
displayname: STUFF
Views:
-   name: Summary List
    displayname: Summary List
    viewname: summarylist
    displaytype: datatable
    parameters:
        filename: nf_output/result.tsv
        columns:
            -   title: "Filename"
                data: "Filename"
            -   title: "Vendor"
                data: Vendor
            -   title: "Model"
                data: Model
            -   title: "MS1"
                data: MS1
            -   title: "MS2"
                data: MS2
        columnDefs: '[ {"targets": 0,"data": null,"render": function ( data, type, row, meta ) {
            return `
            <a target="_blank" href="/dashboards/lcms?usi=mzspec:GNPS2-${task}:${encodeURIComponent(row["Filename"])}">View LCMS - ${row["Filename"]}</a>
            `;}}]'
            
